# Verification

Scenarios and their current state. Levels: **implemented** · **tested locally** ·
**tested in integration** · **verified in the real environment** · **not yet verified**.
"Local" here means Kit's PC (Postgres 18, `npm run dev`); nothing is deployed yet.

| # | Scenario | Level | Last checked | How / result |
|---|---|---|---|---|
| 1 | Scripture data: all five standard works load with correct verse counts | tested locally | 2026-09-13 | `npm run build:data` → OT 23,145 · NT 7,957 · BoM 6,604 · D&C 3,654 · PGP 635 = 41,995 (script fails on any mismatch) |
| 2 | Reader: library → book → chapter, headings, cross-references, prev/next | tested in integration | 2026-09-13 | curl against the app: `/scriptures/bofm/1-ne/3` has verse 7, John 3:16 cross-refs include Romans 5:8, Malachi 4 → Matthew 1. The earlier dev-server 500s on chapter pages (memory pressure, 1.3 GB free) are gone: after a restart with 3.6 GB free, `/scriptures/ot/prov/3` and `/scriptures/ot/eccl/11` returned 200 |
| 3 | Invite-only accounts: invite sign-up, public sign-up closed, sign-in rate limit, member pages protected | tested in integration | 2026-09-13 | curl: `POST /api/auth/sign-up/email` → 403 and no user row; 6th wrong password in a minute → 429; signed-out `/feed` `/admin` `/talks` → 307 to sign-in. Browser walkthrough on local dev: invite sign-up → feed, member can't see Invites |
| 4 | Access control: posts, comments, reactions, invites, talks, talk items | tested locally | 2026-09-13 | `npm test` → 109/109 pass (access-control, talks-usage, talk-items suites: other member can't read/change/delete; single-use and expiring invites; forged ids refused) |
| 5 | Feed: share verse or range, comments, reactions, delete with confirm | tested in integration | 2026-09-13 | Local browser walkthrough (share 1 Ne 3:7, react, comment, delete); curl as a throwaway member for range posts (Moroni 10:4–5) |
| 6 | Search: all-words then some-words, phrases, volume filter | tested locally | 2026-09-13 | `tests/search.test.ts` 6/6 ("charity never faileth" fallback, `"strait gate"`, bofm filter) |
| 7 | Study assistant cites only real verses (bracketed or plain) | tested in integration | 2026-09-13 | Live calls through the local app as throwaway users: Gemini Find answers 7/7 and 6/6 citations real; `tests/citations-plain.test.ts`, `tests/live-regressions.test.ts` pass |
| 8 | Provider fallback chain (Gemini → Groq → OpenRouter) | tested in integration | 2026-09-13 | Live: Cerebras 402 → Groq, Gemini 3.8 429 → Gemini 3.6, Groq 429 → OpenRouter; `tests/fallback-model.test.ts` pass |
| 9 | Assistant declines to write or outline talks (principle 8) | tested locally | 2026-09-13 | `tests/no-talk-writing.test.ts` pass. The live refusal was observed **before** the outline mode was removed; not re-run live since |
| 10 | Home page: feed beside assistant, stacked on phones | tested in integration | 2026-09-13 | Browser (read-only) iframe measurements: 1280px side by side (672/416px), 390px stacked. **Measured before the Come, Follow Me card was added above the grid**; layout not re-measured since |
| 11 | Talk builder T1: capsules render in order, owner-only | tested in integration | 2026-09-13 | curl as a throwaway member: all four capsule kinds in order with Alma 32:21 text, another member's talk → 404; `tests/talk-items.test.ts` 7/7 |
| 12 | Talk builder T1: drag to reorder (mouse, touch, keyboard) and autosave in a browser | tested locally (Kit, browser) | 2026-09-14 | Kit tried the talk feature in his browser on local dev and reported "it works". He didn't list which interactions he tried, so touch and keyboard dragging aren't separately confirmed |
| 13 | Add to talk (T2): buttons members-only; adding lands a capsule | tested in integration (buttons) · tested locally (Kit, browser) | 2026-09-14 | curl: Search shows 50 buttons signed in, 0 signed out; `/api/passages` returns `id`/`endId`. Covered by Kit's 2026-09-14 browser test of the talk feature ("it works"), not itemized |
| 14 | Production build | tested locally | 2026-09-13 | `npx next build` exit 0 (before the Come, Follow Me card and rename; not re-run since) |
| 15 | Deploy to VPS (`knit.marzipan-solutions.com`) | not yet verified | 2026-09-14 | Not started. Subdomain changed from `scriptures.` on 2026-09-14 (D5). After Kit renamed the Cloudflare record, 1.1.1.1 and 8.8.8.8 both resolve `knit.` to the VPS (the same address as `recipes.`, so DNS only, not proxied), and `scriptures.` is NXDOMAIN on both. The rate-limit client-IP gap is closed locally (row 19); re-check it through Caddy at deploy |
| 16 | Come, Follow Me week card on the signed-in home page | tested in integration | 2026-09-13 | `tests/come-follow-me.test.ts` 9/9 (Monday turnover in America/New_York, no schedule gaps, every reading resolves to a real chapter). curl on dev as a throwaway member: "Come, Follow Me · September 7–13", "He Shall Direct Thy Paths" linking to lesson `/37`, six reading links (Proverbs 1–4 … 11–12); signed-out home shows no card. Not looked at in a browser; the Church lesson link wasn't opened (the site is never fetched) |
| 17 | App renamed to Knit | tested in integration | 2026-09-13 | curl signed-out home on dev: `<title>Knit</title>`, header and welcome heading say Knit; grep finds no "Scripture Study" left; `npm run typecheck` clean. Invite page heading changed but not loaded |
| 18 | Talk builder T3: print view built from capsules, owner-only | tested in integration · tested locally (Kit, browser) | 2026-09-14 | curl on dev as a throwaway member: capsules added in reverse and then reordered print in the arranged order (heading → Moroni 10:4–5 with full verse text and note → thought with line break → link label and URL); no markdown block left; another member's talk → 404; signed out → 307; builder page has the "Print view" link. `npm test` 109/109, typecheck clean. Kit then tried the talk feature in his browser and reported "it works" (not itemized, so print preview hiding the header/footer isn't separately confirmed) |
| 19 | Sign-in rate limit keys on each member's real IP behind a proxy (forged leftmost `X-Forwarded-For` ignored) | tested in integration | 2026-09-14 | Root cause found in better-auth 1.7.4's `getIP`: without `trustedProxies`, any multi-address `X-Forwarded-For` is dropped and every such request shares one "no-trusted-ip" bucket (dev hides it by falling back to 127.0.0.1). Next only sets the header when it's missing (`base-server.js:612`). `tests/rate-limit-ip.test.ts` failed before the fix (same client via a single header got 401, not 429) and passes after `advanced.ipAddress.trustedProxies: ["127.0.0.1", "::1"]`; `npm test` 110/110. Live curl on dev: client A with a rotating forged leftmost address → 401×5 then 429; client B behind a chain → 401. **Not yet verified through the real Caddy on the VPS** |
