# Verification

Scenarios and their current state. Levels: **implemented** · **tested locally** ·
**tested in integration** · **verified in the real environment** · **not yet verified**.
"Local" here means Kit's PC (Postgres 18, `npm run dev`); nothing is deployed yet.

| # | Scenario | Level | Last checked | How / result |
|---|---|---|---|---|
| 1 | Scripture data: all five standard works load with correct verse counts | tested locally | 2026-09-13 | `npm run build:data` → OT 23,145 · NT 7,957 · BoM 6,604 · D&C 3,654 · PGP 635 = 41,995 (script fails on any mismatch) |
| 2 | Reader: library → book → chapter, headings, cross-references, prev/next | tested in integration | 2026-09-13 | curl against the app: `/scriptures/bofm/1-ne/3` has verse 7, John 3:16 cross-refs include Romans 5:8, Malachi 4 → Matthew 1. The earlier dev-server 500s on chapter pages (memory pressure, 1.3 GB free) are gone: after a restart with 3.6 GB free, `/scriptures/ot/prov/3` and `/scriptures/ot/eccl/11` returned 200 |
| 3 | Invite-only accounts: invite sign-up, public sign-up closed, sign-in rate limit, member pages protected | tested in integration | 2026-09-13 | curl: `POST /api/auth/sign-up/email` → 403 and no user row; 6th wrong password in a minute → 429; signed-out `/feed` `/admin` `/talks` → 307 to sign-in. Browser walkthrough on local dev: invite sign-up → feed, member can't see Invites |
| 4 | Access control: posts, comments, reactions, invites, talks, talk items | tested locally | 2026-09-13 | `npm test` → 109/109 pass (access-control, talks-usage, talk-items suites: other member can't read/change/delete; single-use and expiring invites; forged ids refused) |
| 5 | Feed: share verse or range, comments, reactions, delete with confirm | tested in integration | 2026-09-13 | Local browser walkthrough (share 1 Ne 3:7, react, comment, delete); curl as a throwaway member for range posts (Moroni 10:4–5) |
| 6 | Search: all-words then some-words, phrases, volume filter | tested locally | 2026-09-13 | `tests/search.test.ts` 6/6 ("charity never faileth" fallback, `"strait gate"`, bofm filter) |
| 7 | Study assistant cites only real verses (bracketed or plain) | tested in integration | 2026-09-13 | Live calls through the local app as throwaway users: Gemini Find answers 7/7 and 6/6 citations real; `tests/citations-plain.test.ts`, `tests/live-regressions.test.ts` pass. **Live on the VPS 2026-09-14:** a throwaway member asked "Scriptures about hearts knit together in unity" in headless Chrome; the answer cited Mosiah 18:21, Colossians 2:2, 1 Samuel 18:1, Moses 7:18, Acts 4:32, D&C 38:27 (all real) with no errors; the journal shows `gemini-flash-latest`, 5 steps, 11,761 tokens, **68.0s** (slow; a post-launch item) |
| 8 | Provider fallback chain (Gemini → Groq → OpenRouter) | tested in integration | 2026-09-13 | Live: Cerebras 402 → Groq, Gemini 3.8 429 → Gemini 3.6, Groq 429 → OpenRouter; `tests/fallback-model.test.ts` pass |
| 9 | Assistant declines to write or outline talks (principle 8) | tested locally | 2026-09-13 | `tests/no-talk-writing.test.ts` pass. The live refusal was observed **before** the outline mode was removed; not re-run live since |
| 10 | Home page: feed beside assistant, stacked on phones | tested in integration | 2026-09-13 | Browser (read-only) iframe measurements: 1280px side by side (672/416px), 390px stacked. **Measured before the Come, Follow Me card was added above the grid**; layout not re-measured since |
| 11 | Talk builder T1: capsules render in order, owner-only | tested in integration | 2026-09-13 | curl as a throwaway member: all four capsule kinds in order with Alma 32:21 text, another member's talk → 404; `tests/talk-items.test.ts` 7/7 |
| 12 | Talk builder T1: drag to reorder (mouse, touch, keyboard) and autosave in a browser | tested locally (Kit, browser) | 2026-09-14 | Kit, in his browser on local dev: dragged scriptures and notes into a new order and it worked ("all worked"). Keyboard dragging and touch on a phone weren't mentioned, so they aren't separately confirmed |
| 13 | Add to talk (T2): buttons members-only; adding lands a capsule | tested in integration (buttons) · tested locally (Kit, browser) | 2026-09-14 | curl: Search shows 50 buttons signed in, 0 signed out; `/api/passages` returns `id`/`endId`. Covered by Kit's 2026-09-14 browser test of the talk feature ("it works"), not itemized |
| 14 | Production build | tested locally | 2026-09-14 | `npm run build` (prebuild regenerates `data/scriptures`) exit 0 on Windows, all 21 routes including `/reset/[token]`, after every change through the password-reset slice |
| 15 | Deploy to VPS (`knit.marzipan-solutions.com`) | verified in the real environment | 2026-09-14 | BOOTSTRAP §0–6 run on the VPS (Ubuntu 24.04, Node 22.23.2, Postgres 16.15, Caddy 2.11.4). `knit` system user, repo cloned at `f1d1616`, `knit` role and database, `.env` 600 with a server-generated DB password and auth secret and the three AI keys copied from Kit's `.env.local` without printing them (lengths 53/56/73 match). `db:migrate` needed `apt-get install unzip` first (`spawnSync unzip ENOENT`); then migrations 001–006 applied and `verses` = 41,995. `next build` passed (21 routes). `knit.service` active, listening on 127.0.0.1:3102 only. Caddyfile backed up, block appended, `caddy validate` ok, reloaded. From Kit's PC over the internet: `/sign-in` 200 with `<title>Sign in · Knit</title>` and "Mosiah 18:21", http → 308 to https, signed-out `/feed` → 307 to sign-in, a chapter 200, public sign-up 403. Neighbors unaffected: postgres, caddy, ff-api/web, recipe-chat-api/web kept their earlier start times (installing unzip restarted nothing), and recipes. and fantasyfootball. return 200. §7: a one-time admin invite for Kit was created with `npm run invite` as `knit` (1 open admin invite in the DB; the link was handed to Kit in chat and isn't recorded here). Kit created his account from it (the only user, role `admin`; the admin invite shows used by him). §9 on the live site, headless Chrome at 390px with a fresh profile and a throwaway member: a member invite sign-up → `/` "Welcome, Launch" with no Invites link, the invite recorded as used and the role `member`; the assistant answered one question with real citations (below, row 7); a reset link → new password → `/sign-in?reset=1` notice → sign-in with the new password; the reused link says already used. The test member and its invite were deleted (users back to 1; no sessions, accounts, or reset rows left). **Not yet:** Kit's own phone check, and the first scheduled backup (2026-09-15 04:45 UTC) |
| 16 | Come, Follow Me week card on the signed-in home page | tested in integration | 2026-09-13 | `tests/come-follow-me.test.ts` 9/9 (Monday turnover in America/New_York, no schedule gaps, every reading resolves to a real chapter). curl on dev as a throwaway member: "Come, Follow Me · September 7–13", "He Shall Direct Thy Paths" linking to lesson `/37`, six reading links (Proverbs 1–4 … 11–12); signed-out home shows no card. Not looked at in a browser; the Church lesson link wasn't opened (the site is never fetched) |
| 17 | App renamed to Knit | tested in integration | 2026-09-13 | curl signed-out home on dev: `<title>Knit</title>`, header and welcome heading say Knit; grep finds no "Scripture Study" left; `npm run typecheck` clean. Invite page heading changed but not loaded |
| 18 | Talk builder T3: print view built from capsules, owner-only | tested in integration · tested locally (Kit, browser) | 2026-09-14 | curl on dev as a throwaway member: capsules added in reverse and then reordered print in the arranged order (heading → Moroni 10:4–5 with full verse text and note → thought with line break → link label and URL); no markdown block left; another member's talk → 404; signed out → 307; builder page has the "Print view" link. `npm test` 109/109, typecheck clean. Kit then opened the browser print preview on local dev and reported it worked |
| 19 | Sign-in rate limit keys on each member's real IP behind a proxy (forged leftmost `X-Forwarded-For` ignored) | verified in the real environment | 2026-09-14 | Root cause found in better-auth 1.7.4's `getIP`: without `trustedProxies`, any multi-address `X-Forwarded-For` is dropped and every such request shares one "no-trusted-ip" bucket (dev hides it by falling back to 127.0.0.1). Next only sets the header when it's missing (`base-server.js:612`). `tests/rate-limit-ip.test.ts` failed before the fix (same client via a single header got 401, not 429) and passes after `advanced.ipAddress.trustedProxies: ["127.0.0.1", "::1"]`; `npm test` 110/110. Live curl on dev: client A with a rotating forged leftmost address → 401×5 then 429; client B behind a chain → 401. **Through the real Caddy (2026-09-14):** from Kit's PC with a rotating forged `X-Forwarded-For`, 401×5 then 429; a second client (the VPS itself, through Caddy) still got 401 right after; the journal shows no "could not determine a client IP" warning |
| 20 | Admin password-reset link: admin-only, single-use, expiring, signs the member out everywhere | verified in the real environment (reset form) · not yet verified (the admin "Create reset link" button) | 2026-09-14 | `tests/password-resets.test.ts` 9/9 (new password works and old fails; reuse, simultaneous submits, expired and made-up links refused; members and made-up ids can't create links, checked against the DB role; too-short password doesn't use up the link; sessions deleted; a new link cancels the old one); `npm test` 119/119. On dev as throwaway users: signed-out `/reset/<token>` shows "For RS member" and the form; a made-up token shows the invalid message; admin `/admin` lists the member under "Reset a password"; member `/admin` → 307 to `/feed`; after resetting (through `lib/password-resets.ts`, not the browser form), the member's existing session → 307 to sign-in, old password 401, new password 200, and the link page says already used; `/sign-in?reset=1` shows the notice. The admin's "Create reset link" button and the reset form's submit weren't clicked in a browser. **Live, 2026-09-14:** on knit.marzipan-solutions.com, a link made through `lib/password-resets.ts` as Kit's admin id, submitted in headless Chrome: new password saved, redirected to `/sign-in?reset=1` with the notice, signed in with the new password, and the reused link says already used. The `/admin` button itself wasn't clicked (that needs Kit's session) |
| 21 | Deploy prep: `deploy/` (systemd unit, Caddy snippet, deploy script, nightly backup, env template, BOOTSTRAP runbook) | verified in the real environment (unit, Caddy, backup) | 2026-09-14 | `bash -n` clean on `deploy.sh` and `backup_db.sh`. Backup script run on Kit's PC against the dev database (`DB`/`BACKUP_DIR` overrides): exit 0, 2.4 MB valid gzip containing `user`, `session`, `invites`, `posts`, `talks`, `talk_items`, `password_resets`, `verses` with all 41,995 verse rows. An unreachable database → exit 1, and a real-but-empty database (381 bytes) → exit 1 "only 381 bytes"; neither leaves a file (dump goes to `.partial`, renamed only after the size check). Test dumps deleted. On the VPS 2026-09-14: `knit.service` and the Caddy block are live (row 15). The backup script is installed as `/usr/local/bin/knit-backup.sh` with `/etc/cron.d/knit-backup` (04:45 daily, as postgres, cron active). The manual run as postgres wrote `/var/backups/knit/knit-20260914-122556.sql.gz`, 2,400,962 bytes, which passes `gunzip -t` and contains `user`, `invites`, `posts`, `talks`, `talk_items`, `password_resets`, `verses`, with no `.partial` left over. The first scheduled run (2026-09-15 04:45) isn't confirmed yet. `deploy.sh` hasn't been run |
| 22 | Layout/color fixes from the Codex audit, plus the larger name with Mosiah 18:21 | tested in integration (headless Chrome at phone width) · not yet verified (Kit's real phone) | 2026-09-14 | Contrast script over the final tokens, light and dark: every text pair (fg, muted, accent, error on bg, card, hl) ≥4.52:1, `--control` and `--error` edges ≥3.11:1, 0 failures. `npm run typecheck` clean, `npm test` 119/119, `npm run build` exit 0. Production server + headless Chrome through the DevTools protocol at 390px (mobile, 2x), as a throwaway admin with a long-titled talk and a post: no horizontal overflow on `/sign-in`, `/scriptures`, `/search`, a chapter, `/`, `/feed`, `/talks`, a talk, its print view, `/study`, `/admin`, or a post, in light or dark. Fields, primary and secondary buttons, nav links, reactions, reader chips, and builder add buttons all measure ≥44px. The "More" menu opens with Assistant / My talks / Invites at 44px each. Print media on a dark-preferring device renders dark text on white with no header or footer. Screenshots were reviewed by eye. **Still under 44px** (text links, out of this slice's scope): verse-reference links in cards (19px) and search results (24px), "N comments", "Delete", capsule "Remove" (16px), breadcrumbs, "Print view", "Full page →" |
| 23 | Study assistant keeps the conversation when you leave and come back; cited verses open in a popup | tested locally (unit + headless Chrome on dev) · deployed · not yet verified (on the live site, Kit's phone) | 2026-09-14 | Bug Kit found on the live site: tapping a verse in an answer navigated to the reader, and Back lost the chat (it lived only in React state). `tests/assistant-history.test.ts` 6/6 (round trip with draft, per-member keys, last 30 messages, corrupt data dropped, empty chat clears, throwing or missing storage); `npm test` 125/125; typecheck clean. Headless Chrome at 390px on dev as a throwaway member (deleted): a real question (78s) produced a chip, and tapping it opened the popup with Mosiah 18:21's text while staying on `/study`. Then with a saved chat preloaded: the restore on load showed 3 bubbles; the popup's Close, Add to talk, and Read the chapter are 44px; it reopens after Escape, ×, and a backdrop tap; `aria-labelledby` ids are unique; Read the chapter → `/scriptures/bofm/mosiah/18#v21`, and Back → `/study` with all 3 bubbles; a reload keeps it; "Start over" clears it (starters shown again). Screenshot reviewed. **Found and fixed during testing:** the first version synced close back to React state, and a late `close` event after Escape swallowed the next tap, so the same verse wouldn't reopen. **Deployed 2026-09-14:** pushed `5ca7624` and ran `deploy.sh` on the VPS: fast-forwarded (12 files), migrations and the 41,995-verse reload re-ran cleanly, the build compiled, knit restarted at 12:50:53 UTC and `/sign-in` returned 200. From Kit's PC: `/sign-in` 200, signed-out `/study` → 307 to sign-in, a chapter 200; recipes. and fantasyfootball. 200; no errors in the journal since the restart. The popup itself hasn't been exercised on the live site |
| 24 | Free-form family posts: text alone, or with an optional scripture; empty posts refused | tested locally (unit + headless Chrome on dev) · deployed · not yet verified (posting on the live site) | 2026-09-14 | Bug Kit found: every post had to start from a scripture. Migration 007 applied locally. `tests/posts-freeform.test.ts` 7/7 (text-only post saved with `verse_id` null and shown in the feed; a typed scripture attaches, ranges included; the reader's reference text round-trips to the same ids for 1 Ne 3:7 and Moroni 10:4–5; a post needs text or a scripture, and a link alone isn't enough; a made-up reference is an error; only https links, and bodies up to 5,000 characters; the DB itself refuses an empty post (`posts_has_content`) and a range without a start (`posts_range_needs_start`)); `npm test` 132/132; typecheck clean. Headless Chrome at 390px on dev as a throwaway member (deleted, with its posts): an empty post → "Write something, or add a scripture."; "Hezekiah 3:1" → the not-found message, with the typed text and reference kept; a text-only post → `/feed`, at the top, with no verse card; text + Alma 32:21 → verse card; the reader's Share on 1 Ne 3:7 → `/share?v=1-ne.3.7` with the preview and "1 Nephi 3:7" filled in → posted → home shows it with its card; no horizontal overflow; feed screenshot reviewed. **Deployed 2026-09-14:** pushed `c9c174c` (scan clean), `deploy.sh` on the VPS exit 0, knit restarted 13:25:03 UTC. (It actually ran twice: the journal shows a first run restarting knit at 13:23:49, from an SSH call Kit had stopped that went through anyway; both runs deployed the same commit, and the second applied `ASSISTANT_PROVIDERS=gemini,groq`.) The live DB records `007_freeform_posts.sql`, `posts.verse_id` is nullable, and the checks `posts_has_content` and `posts_range_needs_start` exist. The built server contains the new composer strings and none of the old share form. From Kit's PC: `/sign-in` 200, signed-out `/feed` and `/share` → 307 to sign-in, recipes. and fantasyfootball. 200 |
| 25 | Editing a post: author only, marked "edited", scripture can be added, changed, or removed, never emptied | tested locally (unit + headless Chrome on dev) · deployed · not yet verified (editing on the live site) | 2026-09-14 | Kit asked for a way to fix typos. `tests/posts-edit.test.ts` 6/6 (the author fixes a typo and `edited_at` is set; scripture added → changed to a range → removed; another member can't edit, even claiming admin; a real admin can't rewrite someone else's post; an edit can't empty a post (app message, and the DB check `posts_has_content`); a missing post id changes nothing); `npm test` 138/138; typecheck clean. Headless Chrome at 390px on dev with two throwaway members (deleted): the author's post showed an Edit link → `/posts/224/edit` with "Edit post", the text pre-filled, and "Save changes"; fixing the typo + adding Alma 32:21 → `/posts/224` with the fixed text, the verse card, and "· edited"; editing again pre-filled the scripture; emptying both fields → "Write something, or add a scripture." and stayed on the edit page; clearing the scripture → card without a verse. The other member: saw the post with no Edit or Delete, and `/posts/224/edit` → 404 with no form. Comment editing isn't built. **Deployed 2026-09-14:** `d12ee8d` via `deploy.sh` (exit 0, knit restarted 13:43:13 UTC, no new migration). The built server contains the edit page and the new strings; no app errors in the journal. From Kit's PC: `/sign-in` 200, signed-out `/feed` and `/posts/1/edit` → 307 to sign-in, recipes. and fantasyfootball. 200 |
| 26 | Home page: the family feed comes first, with posts on the first screen | tested locally (headless Chrome on dev) · deployed · not yet verified (Kit's phone) | 2026-09-14 | Kit: the feed "gets hidden at the bottom". Cause: the assistant panel had `max-lg:order-first`, so phones showed the Come, Follow Me card, then the whole assistant, then the feed, and the open composer pushed posts further down. Change: the assistant follows the feed on phones, and the home composer is a one-line "Write a post…" bar (`<details>`) that opens on tap (the Feed page keeps the full composer). Measured on dev with a throwaway member and 3 posts (deleted), page y-offsets before → after: **390px** feed heading 738 → 328, first post 1164 → 434 (now on the first 844px screen), assistant 345 → 1190; **1280px** first post 754 → 434, feed heading unchanged at 328, assistant stays sticky at the right; no horizontal overflow. Tapping the bar opened the composer, and posting from home landed the new post on top. Screenshot reviewed. Typecheck clean; `npm test` 138/138. (The script's "composer visible" check was wrong: Chrome still reports a size for content inside a closed `<details>`. The y-offsets are the evidence it's collapsed). **Deployed 2026-09-14:** `d12ee8d` via `deploy.sh` (exit 0, knit restarted 13:43:13 UTC, no new migration). The built server contains the edit page and the new strings; no app errors in the journal. From Kit's PC: `/sign-in` 200, signed-out `/feed` and `/posts/1/edit` → 307 to sign-in, recipes. and fantasyfootball. 200 |
| 27 | Reactions on comments (❤️ 🙏 💡), like posts | tested locally (unit) · deployed · not yet verified (in a browser) | 2026-09-14 | Kit asked for emojis on comments. Migration 008 (`comment_reactions`, cascades with the comment and the member, same three kinds) applied locally. `tests/comment-reactions.test.ts` 4/4 (toggle on and off, with shared counts and per-viewer "mine"; comment reactions don't touch the post's; a made-up comment id returns null; reactions are deleted with their comment and other kinds are refused by the DB check); `npm test` 142/142; typecheck clean. Post and comment buttons now share `components/ReactionButtons.tsx`. Not built or clicked locally: Kit wanted the dev server off and the PC was low on memory, so the VPS build and a live check will be the first real render. **Deployed 2026-09-14:** `605bd71` via `deploy.sh` (exit 0, `applied 008_comment_reactions.sql`, knit restarted 16:40:14 UTC). On the live DB, `comment_reactions` has both FKs `ON DELETE CASCADE`, the kind check (heart, pray, insight), and the `(comment_id, user_id, kind)` primary key, and the knit role can SELECT/INSERT/DELETE it. The built server references the table; no app errors in the journal. From Kit's PC: `/sign-in` 200, signed-out `/feed` and `/posts/1` → 307 to sign-in, recipes. and fantasyfootball. 200. No live browser check with a throwaway member (Kit approved the push and deploy only) |
| 28 | Opt-in push: new family posts and comments on your posts | tested locally (unit + production build) · deployed · not yet verified (actual device delivery) | 2026-09-24 | Migration 009 applied locally. `tests/push-notifications.test.ts` 3/3: a new post reaches all of every other member's subscribed devices but not the author; a comment reaches only the post author's devices and self-comments send nothing; a browser endpoint follows the currently signed-in member and disabling is device-scoped. Full `npm test` 145/145, typecheck clean, `npx next build` exit 0 with `/notifications`, `/manifest.webmanifest`, and generated PNG icon routes. Push failures run after the response and cannot prevent a post/comment; 404/410 endpoints are deleted. The lock-screen message has the actor's name but no post/comment text. **Deployed 2026-09-24:** pushed `8fc3d26`; `deploy.sh` generated three non-empty VAPID settings in the `knit`-owned mode-600 `.env` without printing them, applied migration 009, reloaded 41,995 verses, built 17 static pages/routes, restarted Knit, and `/sign-in` returned 200. VPS checks: service active, `.env` is `knit 600`, exactly three push settings are non-empty, `push_subscriptions` exists and is owned by `knit`, and there are no error-priority journal entries since deploy. Public HTTPS checks: manifest, service worker, and both PNG icons return 200 with the expected MIME types; `/sw.js` has no-store and self-only CSP headers; signed-out `/notifications` redirects to sign-in. The recipes and fantasy-football neighbors still return 200. Browser permission and an actual delivery still need a member to opt in. |
